
Exam App (React + Vite)
-----------------------
Instructions:
1. Replace QUESTIONS_URL in src/App.jsx if you want to load questions from a remote raw URL.
2. Alternatively, keep questions.json in the project root (already included).
3. Deploy by pushing the repository to GitHub and import the repo in Vercel.
